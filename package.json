{
  "name": "earmms-sf",
  "version": "1.1.0",
  "dependencies": {
    "@cap-js/hana": "^2",
    "@sap/cds": "^9",
    "@sap/xssec": "^4.13.0"
  },
  "devDependencies": {
    "@cap-js/sqlite": "^2.4",
    "@sap/cds-dk": "^9.9.1"
  },
  "scripts": {
    "start": "cds-serve",
    "build": "cds build --production"
  },
  "private": true,
  "engines": {
    "node": ">=20"
  },"cds": {
    "requires": {
      "auth": "dummy",
      "[production]": {
        "auth": "dummy"
      }
    }
  }
}
